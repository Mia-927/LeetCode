//first attempt on solving 14. Longest Common Prefix
class Solution {
    public String longestCommonPrefix(String[] strs) {
        String aws = "";
        int min = strs[strs.length - 1].length();
        for(int i = 0; i < strs.length; i ++){
            if(min > strs[i].length()){//間違えてmax逆求めてた
                min = strs[i].length();
            }
        }
        for(int i = 0; i < min; i ++){
            //index bugにならないようにするの苦手
            char c = strs[0].charAt(i);
            for(int j = 0; j < strs.length; j ++){
                if(strs[j].charAt(i) != c){
                    return aws;
                }
                
            }
            aws += c;
        }
        return aws;
    }
}
//same attempt but with ChatGPT
class Solution {
    public String longestCommonPrefix(String[] strs) {
        if(strs == null || strs.length == 0) return ""; //ぬるの場合
        for(int i = 0; i < str[0].length(); i ++){//これが1番長かったらどうするん？
            char c = strs[0].cahrAt(i);//1つ目の単語から
            for(int j = 1; j < strs.length; j++){
                if(i >= strs[j].length() || strs[j].charAt(i) =! c){
                    //前者もしStringの長さ（基本）が早い方のｊの長さを超えたら<-ここで
                    return strs[0].subString(0, i);
                }
            }
            return strs[0];//どゆこと？
        }
    }
}
